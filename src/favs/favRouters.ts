// import { Router } from "express";
// import { createFav, deleteFav, getFav, getFavById } from "./controllers/favControllers";


// const favRouter: Router = Router();

// favRouter.get('/api/fav', getFav);

// favRouter.post('/api/fav', createFav);

// favRouter.get('/api/fav/:id', getFavById);

// favRouter.delete('/api/fav/:id', deleteFav);

// export default favRouter;